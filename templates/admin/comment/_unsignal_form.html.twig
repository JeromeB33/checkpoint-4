<form method="post" action="{{ path('dashboard_comment_unreport', {'id': comment.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir garder ce commentaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="bg-blue-500 mt-4 items-center text-white px-4 py-3 rounded-md focus:outline-none">Valider</button>
</form>
